## 1. Shared utilities

- [x] 1.1 Extract the version comparison helpers (`normalizeVersion`, `splitVersion`, `compareVersions`) from `src/server/version-status.ts` into a shared module so both global and per-project status can reuse them
- [x] 1.2 Add/adjust unit tests covering the extracted comparison helpers (core semver, prerelease ordering)

## 2. Per-project generation version detection

- [x] 2.1 Create a read-only skill scanner module that, given a project root, walks the representative OpenSpec skill directories (e.g. `.claude/skills`, `.opencode/skills`, `.github/copilot/skills`) and returns the first found `metadata.generatedBy` value from a `SKILL.md` frontmatter
- [x] 2.2 Unit-test the scanner: readable version, missing skill file, malformed/absent `generatedBy`, unreadable file, and a guard asserting no project file is modified/created/deleted during detection
- [x] 2.3 Build a per-project version-status service that combines the scanner output with the current global CLI version (from the existing version-status service) into a per-project status (`up-to-date` / `update-available` / `unknown`), and returns `unknown` for every project when the current CLI version is unavailable
- [x] 2.4 Unit-test the service for each status branch and the "current CLI unavailable → all unknown" fallback

## 3. API surface

- [x] 3.1 Define the API response types for per-project version status (project path, generation version, current CLI version baseline, status) in `frontend/src/lib/types/api.ts` and the mirrored server-side types
- [x] 3.2 Add an endpoint (e.g. `GET /api/project-version-status` plus `POST .../refresh`) in `src/server/routes/api.ts`, computing on startup and on manual refresh, sourcing registered projects from `project-registry.listProjects()`
- [x] 3.3 Add integration tests in `src/server/server.integration.test.ts` covering: registered projects present, no registered projects, and partial detection failure (affected project returned as `unknown` without failing the response)

## 4. Frontend API client and store

- [x] 4.1 Add typed client functions in `frontend/src/lib/api.ts` (`getProjectVersionStatus`, `refreshProjectVersionStatus`) mirroring the existing version-status client style
- [x] 4.2 Create a `projectVersionStatus.svelte.ts` store (snapshot, loading, manualRefresh, initialize/refresh) following the `versionStatus.svelte.ts` pattern
- [x] 4.3 Add unit tests for the store/core (status mapping, refresh behavior, tolerating empty/null snapshots)

## 5. Versions page UI

- [x] 5.1 Refactor the "post-upgrade OpenSpec project guidance" block in `frontend/src/lib/components/layout/SettingsView.svelte` from the path list + single command copy into compact per-project rows
- [x] 5.2 Render each row as a status indicator (color/dot) + project label/path (truncated, full path in `title`) + icon-only copy button; avoid heavy per-row borders/boxes to keep the list quiet
- [x] 5.3 Add a command helper producing `openspec update <path>` and quoting the path only when it contains whitespace
- [x] 5.4 Replace the former single `openspec update` command copy with the per-project copy actions; keep `unknown` projects listed and still copyable
- [x] 5.5 Update `frontend/src/lib/components/layout/settingsTab.test.ts` to assert per-project status rendering, per-project copy wiring, path-qualified command generation, and whitespace quoting

## 6. Localization and labels

- [x] 6.1 Add the minimal i18n message keys needed for status labels and guidance (e.g. up-to-date / update-available / unknown and the unknown-means-check hint), regenerate the paraglide messages, and confirm no missing-message errors in `SettingsView.svelte`

## 7. Verification

- [x] 7.1 Run `npm test` and confirm the full suite passes, including the new tests added in this change
- [x] 7.2 Run `openspec validate add-project-version-status --strict` and confirm it passes
- [x] 7.3 Sanity-check the UI locally with at least one registered project whose skills were generated by the installed OpenSpec CLI, confirming the correct status and copyable path-qualified command
