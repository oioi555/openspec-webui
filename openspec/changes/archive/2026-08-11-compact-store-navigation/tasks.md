# Tasks: Compact Store Navigation

## 1. Compact relationship navigation bar (Dashboard.svelte)

- [x] 1.1 In `frontend/src/lib/views/Dashboard.svelte`, replace the Store Relationship card template block (currently `{#if storeRelationship && (storeRelationship.pointerStoreId || storeRelationship.referenceStoreIds.length > 0)}` wrapping a `SurfaceCard` with `SectionHeader` + `space-y-4` body, ~L392-507) with a single compact relationship navigation bar rendered at the same Dashboard-top placement (above Summary Cards). The bar SHALL render only when `storeRelationship` is non-null and the project declares a `store:` pointer or `references:`; pure Store roots and unrelated projects keep the existing non-render behavior.
- [x] 1.2 Build the bar as a `SurfaceCard shadow="sm"` containing one `flex flex-wrap items-center gap-3 px-4 py-3` row: pointer store segment (icon + resolved label + status) and references trigger segment (see tasks 2-3). Preserve the existing `FIXED_LABELS.dashboard.storeRelationship`-based aria/visual identification where needed, but remove the `SectionHeader` heading layout and the explanatory description paragraphs (`dashboard_pointer_store_description`, `dashboard_references_description`).
- [x] 1.3 Keep all existing `$derived` state (`storeRelationship`, `mergedRows`, `pointerStoreRow`) and the event handlers `handleOpenPlanningStore` and `handleOpenReferencedStore` unchanged; the bar only re-renders them in the new layout. Do not modify `resolveStoreRelationship()`, `resolveStoreRoot()`, `mergeUnifiedProjectList()`, `projectStore`, `storeDiscoveryStore`, or `ProjectSelector`.
- [x] 1.4 Ensure `references` object entries are handled by `id` only: `referenceStoreIds` continues to drive display, resolution, and validation, and the `remote` field of any reference entry is never read, displayed, or used (no code change expected — verify no `remote` access is introduced).

## 2. Pointer store: inline state + direct Open action

- [x] 2.1 In the pointer segment of the bar, when `pointerStoreRow` resolves, render the pointer label from `pointerStoreRow.label` (not the raw storeId) with a `Store` icon, truncating with the `truncate` class and `title={pointerStoreRow.path}` for narrow screens. Place a prominent `Button variant="default" size="sm"` labeled with the existing `t(m.dashboard_open_planning_store)` (or localized equivalent) that calls `handleOpenPlanningStore`, so the working Store opens in one operation.
- [x] 2.2 When `pointerStoreRow` does not resolve (unresolved/unavailable), keep the declared pointer Store visible inline in the bar: show a non-blocking status (e.g., `StatusIndicator` unavailable variant or an inline muted label) with the declared `pointerStoreId`, and render the existing non-blocking unavailable/not-found copy (`dashboard_store_discovery_unavailable` vs `dashboard_store_not_found` depending on `storeDiscoveryStore.status`) together with the `OPENSPEC_STORES_GUIDE_URL` link. Do NOT hide the pointer, prompt local spec initialization, or fabricate a local planning root.
- [x] 2.3 Preserve the Store-root header badge behavior: when `storeRelationship?.isStoreRoot` is true, the `dashboard_store_root_badge` Badge stays beside the project name in the header; the bar still renders only for pointer/references and the header badge is shown additionally in that case.

## 3. References: count + Collapsible (default closed) + expanded list

- [x] 3.1 In the references segment of the bar, when `referenceStoreIds.length > 0`, render a `Collapsible.Root` (already used in `explorer-section`) that is **collapsed by default**, with a right-aligned `Collapsible.Trigger` showing `ChevronRight`/`ChevronDown` and a `<Badge variant="secondary">{count}</Badge>` displaying `referenceStoreIds.length` (e.g., `References (3)` via the existing references copy/key or an added i18n key). Do not render any expanded content by default.
- [x] 3.2 Render `Collapsible.Content` below the bar: for each `referenceStoreIds` entry, look up `mergedRows.find((r) => r.storeId === refStoreId)`; for resolved rows show label + `[Open]` ghost `Button size="sm"` calling `handleOpenReferencedStore(refStoreId)` (no storeId/path display). For unresolved entries show the inline non-blocking status (`dashboard_store_discovery_unavailable` vs `dashboard_store_not_found` by `storeDiscoveryStore.status`) with the `OPENSPEC_STORES_GUIDE_URL` link, keeping the entry visible.
- [x] 3.3 Make the bar responsive: use `flex-wrap` so on narrow widths the references trigger wraps below the pointer line, and the expanded reference list stacks vertically (existing per-row layout). Preserve read-only reference semantics — opening a referenced Store only activates its row/root in the unified Project Selector; the current project's planning root is preserved.

## 4. i18n message cleanup

- [x] 4.1 Remove the now-unused description keys `dashboard_pointer_store_description` and `dashboard_references_description` from `frontend/messages/en.json` and every other supported locale file under `frontend/messages/` (de, es, pt-BR, fr, ja, zh-CN, etc.), and from the corresponding generated `frontend/src/lib/paraglide` messages after the message build step if applicable.
- [x] 4.2 Keep the keys still used by the new bar: `dashboard_store_root_badge`, `dashboard_open_planning_store`, `dashboard_store_discovery_unavailable`, `dashboard_store_not_found`, `dashboard_store_unavailable_link`, plus `FIXED_LABELS.dashboard.storeRelationship`. Add any new keys (e.g., a references-count trigger label/aria such as `dashboard_references_count`) to `frontend/messages/*.json` and register them in `frontend/src/lib/uiText.ts` (message-id constants and/or `FIXED_LABELS.dashboard.*`) following the existing i18n conventions; ensure every supported locale has the new keys.
- [x] 4.3 Remove any now-unused `FIXED_LABELS` entries and unused imports from `Dashboard.svelte` (e.g., `Info`, description-related helpers) left over by the card removal, without touching labels still referenced elsewhere.

## 5. Tests

- [x] 5.1 Update `frontend/src/lib/views/dashboardStore.test.ts` for the compact bar structure: assert the relationship renders as a single bar at Dashboard top above Summary Cards, pointer Store label + `Open planning Store` button appear inline, the header Store-root badge still renders in the `h1` only, and the card/description paragraphs (`dashboard_pointer_store_description`, `dashboard_references_description`) are no longer rendered.
- [x] 5.2 Cover the references states in `dashboardStore.test.ts` (and `dashboard.test.ts` where applicable): 0 references (trigger/count hidden), a typical count (e.g., 3, shown collapsed as `References (3)`), and a ~10-entry list (expand shows all 10 rows, each with `[Open]`); assert the `Collapsible` content is closed by default and opens on trigger activation.
- [x] 5.3 Cover pointer-only, references-only, and pointer+references combined relationships, plus unresolved states: unresolved pointer stays visible inline with unavailable/not-found copy and the Stores Guide link, unresolved reference entries stay visible in the expanded list, and no test regresses the "never suggests local initialization" / "no card for pure Store root" guarantees.
- [x] 5.4 Update/keep the merged-rows and navigation tests: referenced Store `[Open]` still calls the bind/add-project path via `handleOpenReferencedStore`, `handleOpenPlanningStore` still resolves/binds the pointer Store, and `remote` is never used in reference handling.

## 6. Verification

- [x] 6.1 Run the frontend unit tests for the affected files (`frontend/src/lib/views/dashboardStore.test.ts`, `frontend/src/lib/views/dashboard.test.ts`) via the repo's configured test command, and confirm all updated and new assertions pass.
- [x] 6.2 Run the frontend typecheck (`npm run check` or the repo-standard typecheck script) and the frontend build (`npm run build`) to confirm no type or build errors from the Dashboard.svelte/i18n changes.
- [x] 6.3 Optionally run `openspec validate compact-store-navigation` (or repo-standard OpenSpec validation) and confirm proposal/spec/tasks artifacts are coherent; final verification ownership rests with the parent agent.
