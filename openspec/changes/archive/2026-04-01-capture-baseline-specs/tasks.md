## 1. Capture the baseline artifacts

- [x] 1.1 Finalize the proposal, design, and capability delta specs for the reverse-engineered baseline.
- [x] 1.2 Add `openspec/project.md` so the repository has project-level OpenSpec context.

## 2. Sync the baseline into the main spec set

- [x] 2.1 Validate the baseline change artifacts with OpenSpec.
- [x] 2.2 Archive the baseline capture change so the new capabilities become the main specs.
- [x] 2.3 Validate the resulting main specs and archived history after the sync.
